<% @messages.each do |message| %>
<div class="col-lg-4 col-md-6">
	<div class="message">	
		<h2><%= message.title %></h2>
		<%= link_to "Lire message", message_path(message), class: "btn btn-default" %>
	</div>
</div>
<% end %>
<div class="col-lg-4">
		<div>
			<%= link_to "Nouveau Message", new_message_path,  class: "btn btn-default" %>
		</div>
</div>